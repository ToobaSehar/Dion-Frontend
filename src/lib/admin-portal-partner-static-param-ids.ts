/**
 * Paths pre-rendered for `/admin/portal-figma/partners/[partnerId]` when using `output: 'export'`.
 * Keep in sync with mock row `id` values in `AdminPortalPartnersView` (`MOCK_ROWS`).
 */
export const ADMIN_PORTAL_PARTNER_STATIC_PARAM_IDS = ['1', '2', '3', '4', '5', '6'] as const;
