/**
 * Paths pre-rendered for `/partner/dashboard/offers/[offerId]` when using `output: 'export'`.
 * Mock offer row ids from `PartnerMyOffersView` (`DEFAULT_OFFERS`) that have detail pages.
 */
export const PARTNER_OFFER_DETAIL_STATIC_PARAM_IDS = ['1', '2', '3', '4', '5'] as const;
