/**
 * **Late offer** pill — pale amber surface, deep amber label (admin partner-offer tables).
 * Import these tokens when composing custom layouts; use {@link LateOfferBadge} for the standard mark.
 */
export const LATE_OFFER_BADGE_FRAME =
  'font-avenir-regular inline-flex items-center gap-1 rounded-full bg-[#FEF6EE] px-2 py-0.5 text-[11px] font-semibold leading-[18px] text-[#B54708]';

export const LATE_OFFER_BADGE_ICON_CLASS = 'size-3 shrink-0 text-[#B54708]';
